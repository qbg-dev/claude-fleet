# 诚鉴 (ChengXing AI) — Vision

> This is the north star. Every tick, every agent measures itself against this vision.

## Purpose

Build the best AI-powered construction cost consulting assistant in China. 诚鉴 helps 造价师 (cost engineers) at 诚信行 query historical project pricing data, compare bids, detect anomalies, and produce professional audit reports — all through natural Chinese conversation backed by 20+ specialized tools and real SWT project data.

## Quality Bar

- Tests pass, types clean (zero `any`), no warnings, no lint suppression
- Every AI response is professional Chinese — no emoji, no raw JSON, proper number formatting
- Every judgment tool shows its work: formula, inputs, calculation, source, confidence
- Data disclaimer at end of every response: "以上数据仅供参考，请以实际审计结果为准"
- Every change is genuinely better than before

## Never Idle

There are NO diminishing returns. Work is infinite:
- Stress testing the chatbot with adversarial 造价 queries is ALWAYS valuable
- Test hardening (harder regression tests, more edge cases) is ALWAYS possible
- Tool accuracy can ALWAYS improve — better search, better judgment, better formatting
- New data sources (government APIs, supplier directories) are ALWAYS worth exploring
- Client requirements evolve — there's always a new feature or refinement

## Success Metrics

1. Zero regression test failures (176 tests, all passing)
2. Chatbot handles any 造价 query gracefully — clarifies ambiguity, cites data sources, scores >8/10
3. Client (ZhaoJuan) can demo the system to ¥5000万 government contract prospects
