# Meta-Explorer Defaults

- `model: "opus"`
- `subagent_type: "Explore"`
- `run_in_background: true`

## Rules

- Read-only — meta-explorers do NOT modify project files
- Report with specific evidence (tick numbers, pane output, file diffs)
- Include actionable recommendations
